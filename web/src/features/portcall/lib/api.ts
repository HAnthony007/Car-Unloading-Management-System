/**
 * DEPRECATED: Use functions from features/portcall/lib/portcalls instead.
 * This shim exists temporarily to avoid breaking imports while we clean up.
 */
export { getPortCallById as fetchPortCallById, getPortCalls as fetchPortCalls } from "./portcalls";

