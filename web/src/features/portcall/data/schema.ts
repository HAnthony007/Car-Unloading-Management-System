export interface PortCall {
  port_call_id: number;
  vessel_agent: string;
  origin_port: string;
  estimated_arrival: string;
  arrival_date: string;
  estimated_departure: string;
  departure_date: string;
  vessel_id: number;
  dock_id: number;
  created_at: string;
  updated_at: string;
}
